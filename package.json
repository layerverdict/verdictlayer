{
  "name": "verdict",
  "version": "0.1.0",
  "private": true,
  "description": "Verdict Layer — Verifiable AI decisions on 0G",
  "packageManager": "pnpm@10.28.2",
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=10.0.0"
  },
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "clean": "turbo run clean && rm -rf node_modules"
  },
  "devDependencies": {
    "turbo": "^2.9.6",
    "typescript": "^5.9.3",
    "@types/node": "^22.19.17",
    "prettier": "^3.8.3"
  }
}
